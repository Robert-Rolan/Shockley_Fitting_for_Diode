# Shockley Equation Fitting 使用说明

## 1. 这是什么

这是一个用于二极管 Shockley 相关 I-V 曲线拟合的图形界面工具。源码仓库包含 Python 程序、测试和一个公开标样数据文件 `control.csv`。

## 2. 从源码运行

1. 安装 Python 3.11 或更新版本。
2. 在项目目录中安装依赖：

```powershell
python -m pip install -r requirements.txt
```

3. 启动程序：

```powershell
python shockley_gui.py
```

也可以在依赖已安装的情况下双击 `start_shockley_fitting_tool.bat`。

## 3. CSV 数据格式

程序读取 `.csv` 文件中的数值数据：

- 第一列作为电压 `V`。
- 最后一列作为电流 `I`。
- 中间列会被忽略。
- 表头或非数值行会自动跳过。
- 至少需要 3 行有效的数值数据。

示例：

```csv
Voltage,Current
-2.0,-1.23e-6
-1.5,-8.50e-7
0.0,1.00e-10
0.5,2.40e-5
1.0,1.10e-3
```

仓库中的 `control.csv` 是公开标样数据，可用于试运行和测试。

## 4. 基本使用流程

1. 点击 `导入 I-V CSV 数据`，选择 CSV 文件。
2. 在 `拟合模型` 中选择要使用的模型。
3. 根据需要调整固定输入、偏压区间拟合权重、参数初值和上下限。
4. 点击 `执行拟合`。
5. 检查右侧图像和下方拟合状态。
6. 点击 `导出拟合数据`，选择导出文件夹。

## 5. 导出结果

导出后会生成两个文件：

- `fitted_curve.csv`：包含原始电流、总拟合电流、各分量电流和残差等曲线数据。
- `fit_parameters.json`：包含拟合参数、固定输入、拟合权重、优化器状态和误差指标。

## 6. 常见问题

如果导入失败，请检查 CSV 至少有两列，并且第一列和最后一列中至少有 3 行可以转换为数字。

如果拟合失败，可以尝试调整参数初值、上下限或偏压区间权重，也请检查输入电流数据中是否存在异常值。

如果需要发布给没有 Python 的用户，可用 PyInstaller 生成 `dist/` 目录，再把整个 `Shockley Equation Fitting` 文件夹打包分发。请不要只移动单独的 `.exe` 文件。
